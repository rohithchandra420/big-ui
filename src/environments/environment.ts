export const environment = {
    URL: 'https://big-api.onrender.com',
    // Kept in sync manually with CHANGELOG.md's latest version — no build
    // tooling currently wires package.json's version into the bundle.
    version: '1.9.1'
};
